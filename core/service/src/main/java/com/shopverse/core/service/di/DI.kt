package com.shopverse.core.service.di

import org.koin.dsl.module

/**
 * Service-layer DI. Concrete Retrofit/Ktor wiring is added in week-2; for now this
 * module exists so the layer's DI surface is wired through the graph.
 */
val serviceDiModule = module {
}
